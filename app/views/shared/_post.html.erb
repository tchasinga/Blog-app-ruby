<div class= "post_card">
<h4>
 <%= link_to "Post ##{post.id }", "/users/#{@user.id}/posts/#{post.id}" %>
 </h4>
      <p> <%= post.title %> </p>
      <p class="post_counter"><span>comments: <%=post.comments_counter%> </span>
       <span>Likes: <%=post.likes_counter%> </span>
       </p>
</div>